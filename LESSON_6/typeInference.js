"use strict";
let typeString = "something"; //The compiler infers the type string for the variable name
let typeNumber = 120; //The compiler infers the type number for the variable name
let typeBoolean = true; //The compiler infers the type boolean for the variable name
